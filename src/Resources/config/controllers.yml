services:
    _defaults:
        public: '%public.services%'
        autowire: false
        autoconfigure: false

    Hanaboso\UserBundle\Controller\UserController:
        class: Hanaboso\UserBundle\Controller\UserController
        arguments:
            - '@hbpf.user.handler.user'
        calls:
            - ['setContainer', ['@service_container']]
